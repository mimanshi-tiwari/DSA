## HINT(s)

There can be otehr solutions but if good to start think form here.

## 'a'.charCodeAt() = 97 ; 'a'.charCodeAt() = 65;  

## If we need to find some type [substring] - SLIDING WINDOW

## Max k element, kth largest, smallest, - HEAP / PRIORITY QUEUE